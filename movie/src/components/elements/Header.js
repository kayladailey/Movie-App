import React from 'react';

//Since it is one line we do not need the 'return' keyword
const Header = () => <div>Header</div>


export default Header; //This is what allows us to use this component in other spots of our App